## 运行环境
- Node.js：建议 22.x
- MongoDB：6.x/7.x/8.x 均可（本地安装或 Docker）

## 项目结构
- `backend/`：Express + MongoDB（用户/权限、数据集、GeoJSON 上传）
- `frontend/`：Vue3 + Element Plus + ArcGIS Maps SDK for JS（`@arcgis/core` 5.0.x）
- `data/`：示例海岸线 GeoJSON（用于种子数据）

## 1. 启动 MongoDB
确保本机 `mongodb://127.0.0.1:27017` 可用。

Docker 示例：

```bash
docker run -d --name mongo -p 27017:27017 mongo:7
```

## 2. 启动后端（3001）
在 `backend/` 下：

1) 复制环境变量文件并修改：
- 将 `backend/.env.example` 复制为 `backend/.env`

2) 安装依赖：

```bash
npm install
```

3) 初始化数据并启动：

```bash
npm run seed
npm run dev
```

健康检查：`GET http://localhost:3001/api/health`

## 3. 启动前端（5173）
在 `frontend/` 下：

1) 将 `frontend/.env.example` 复制为 `frontend/.env`
2) 安装依赖并启动：

```bash
npm install
npm run dev
```

访问：`http://localhost:5173`

### 这些指令分别做什么？
- **`npm install`**：按照 `package.json` 安装依赖（只需要首次安装、或依赖变化时重装）
- **`npm run seed`**：向 MongoDB 写入“演示账号 + 演示数据”（海岸线/全球参考海岸线/监测站点与历史数据）
- **`npm run dev`**：
  - 后端：启动 Express 服务（提供登录、数据集、评论、监测站点 API）
  - 前端：启动 Vite 开发服务器（热更新，便于调试）

## 4. 测试账号
执行过 `backend` 的 `npm run seed` 后会创建：
- 管理员：`admin / admin123`

